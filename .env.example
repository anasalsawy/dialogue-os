# Dialogue-OS / YTA environment template — names only, never real secrets.
# Copy to .env and fill values. Restrict .env to mode 600 under the service user.

# Fixed absolute workspace (do not change once sessions exist)
DIALOGUE_OS_ROOT=/home/azureuser/dialogue-os

# Operator / channel
TELEGRAM_OWNER_ID=
TELEGRAM_CANONICAL_CHANNEL_ID=

# Bot tokens (one consumer per token; long-polling OR webhook, never both)
TELEGRAM_CHIEF_BOT_TOKEN=
TELEGRAM_BUILDER_BOT_TOKEN=
TELEGRAM_RESEARCH_BOT_TOKEN=
TELEGRAM_OPERATIONS_BOT_TOKEN=
TELEGRAM_GROWTH_BOT_TOKEN=
TELEGRAM_CUSTOMER_RELATIONS_BOT_TOKEN=
TELEGRAM_STAGEHAND_BOT_TOKEN=
TELEGRAM_WATCHER_ALPHA_BOT_TOKEN=
TELEGRAM_WATCHER_BETA_BOT_TOKEN=

# Codex CLI control plane
CODEX_CLI_BIN=codex
CODEX_MODEL=
CODEX_TIMEOUT_SECONDS=1800
CODEX_CONTROL_SESSION_KEY=primary
# Authenticate once as the service user with `codex login`, or use the
# documented API-key/access-token login flow. Never commit Codex auth files.

# Temporary Chief backend switch: codex or featherless
CHIEF_BACKEND=codex
CHIEF_FEATHERLESS_BASE_URL=https://api.featherless.ai/v1
CHIEF_FEATHERLESS_API_KEY=
CHIEF_FEATHERLESS_MODEL=
CHIEF_FEATHERLESS_FALLBACK_MODEL=
# Ordered, comma-separated chain. The singular variable above remains supported.
CHIEF_FEATHERLESS_FALLBACK_MODELS=
CHIEF_FEATHERLESS_MAX_OUTPUT_TOKENS=8192

# Fleet-wide Featherless override. When true, Chief and every Hermes specialist
# use only this uncensored primary/fallback chain.
UNCENSORED_FLEET_MODE=false
UNCENSORED_PRIMARY_MODEL=darkc0de/Agent.Xortron
UNCENSORED_FALLBACK_MODELS=darkc0de/XORTRON,ArliAI/Qwen3.5-27B-Derestricted,huihui-ai/Llama-3.3-70B-Instruct-abliterated-finetuned

# Hermes OpenAI-compatible provider (required for non-Chief agents)
# direct calls the inference provider only. agent_api calls a real Hermes Agent
# gateway, retaining Hermes tools, skills, memory, and its internal tool loop.
HERMES_BACKEND=direct
HERMES_BASE_URL=
HERMES_API_KEY=
HERMES_MODEL=
HERMES_MAX_OUTPUT_TOKENS=4096
HERMES_TIMEOUT_SECONDS=120
# chat = separate memory per Telegram chat; profile = one memory per agent
# across Telegram private/group chats, the website, and the War Room.
HERMES_SESSION_SCOPE=chat
HERMES_AGENT_API_URL=http://127.0.0.1:8642
HERMES_AGENT_API_KEY=
HERMES_AGENT_MULTIPLEX_PROFILES=true
# Recommended production topology: independent profile gateways. When set,
# these URLs take precedence over the multiplexed base URL above.
HERMES_AGENT_PROFILE_URLS=
# Shared fleet router. Discovers only chat + tool-use models with a context
# window large enough for Hermes, then quarantines provider/model failures.
HERMES_MODEL_ROTATION_ENABLED=true
HERMES_MODEL_ROTATION_ATTEMPTS=5
HERMES_MODEL_MINIMUM_CONTEXT=65536
HERMES_MODEL_CATALOG_URL=https://api.featherless.ai/v1/models

# Browser tooling (optional; Stagehand/Browserbase is a tool, not a chat backend)
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
STAGEHAND_ENABLED=false

# Active-mission supervision. The supervisor inspects missions whose
# next_check_at (stored in SQLite) has passed, so supervision survives
# restarts. SUPERVISION_TICK_SECONDS=0 disables supervision entirely.
SUPERVISION_TICK_SECONDS=15
MISSION_ACK_TIMEOUT_SECONDS=300
MISSION_HEARTBEAT_TIMEOUT_SECONDS=600
MISSION_OWNER_UPDATE_SECONDS=1800
MISSION_MAX_UNSUPPORTED_CLAIMS=2
MISSION_SUPERVISION_WATCHER=watcher_alpha

# Runtime
DATABASE_PATH=/home/azureuser/dialogue-os/data/dialogue_os.sqlite3
CANONICAL_LOG_PATH=/home/azureuser/dialogue-os/data/canonical/events.jsonl
HEALTH_BIND=127.0.0.1
HEALTH_PORT=8787
# Generate a long random value. This bearer token is shared only with the
# private War Room deployment and must never be committed or logged.
WAR_ROOM_API_TOKEN=
WAR_ROOM_ALLOWED_ORIGINS=https://yta-war-room-live.masryalsawy.chatgpt.site
LOG_LEVEL=INFO
SERVICE_USER=azureuser
BOT_TO_BOT_CHATTER=false
AZURE_LLM_DISABLED=true

# Encrypted state backup (optional; never commit real keys)
# age public recipient, e.g. age1...
BACKUP_AGE_RECIPIENT=
# Destination: file:///path, /path, or s3://bucket/prefix
BACKUP_DESTINATION=
# Private age identity path for restore verification only (never commit the key file)
# BACKUP_AGE_IDENTITY=
